name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy
  - scikit-learn
  - matplotlib
  - qiskit
  - qiskit-aer
  - qiskit-ibm-runtime
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

